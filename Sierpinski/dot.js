class Dot{
    constructor(x, y){
        this.x = x;
        this.y = y;
        ctx.fillRect(this.x - settings.dotSize / 2, this.y - settings.dotSize / 2, settings.dotSize, settings.dotSize)
    }
    render(){
        
    }
}